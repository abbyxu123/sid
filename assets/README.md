# UI Export Assets

Program-ready assets for the CatTV 1.8-inch horizontal screen. Use `ui_export/manifest.json`, `ui_export/manifest.csv`, and `docs/ui_asset_manifest.md` to map assets to pages and backend states.

The original large PNG source folder is not committed to keep the repository light.
